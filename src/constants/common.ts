const ROLE = {
  ADMIN: "ADMIN",
  USER: "USER",
};

const DATABASE_CONNECTION = "BESTFOOD";

const DEFAULT_LIMIT = 15;

const DEFAULT_ORDER_BY = "ASC";

const DEFAULT_PAGE = 1;

const DELIMITER = "|";

const DEFAULT_SORT_BY = "id";

const OTP_KEY = "OTP";

export {
  ROLE,
  DATABASE_CONNECTION,
  DEFAULT_LIMIT,
  DEFAULT_PAGE,
  DELIMITER,
  DEFAULT_ORDER_BY,
  DEFAULT_SORT_BY,
  OTP_KEY,
};
